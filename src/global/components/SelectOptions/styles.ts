import styled from "styled-components"

export const Select = styled.select`
    background: var(--blue);
    color: var(--light);
    border-radius: var(--border-r);
    padding: 1rem;

    cursor: pointer;

    option {
        cursor: pointer;
    }
`